import React from 'react';
import { configs } from '@config';

const DefaultTitle = () => <p className="font-semibole default_font text-4xl text-center mt-5">{configs.SITE_NAME}</p>;

export default DefaultTitle;
